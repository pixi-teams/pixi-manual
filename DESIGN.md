# pixi マニュアル — Design System

> **方向性**: Notion のような *スタイリッシュ・モノトーン・洗練・余白感* を持つ、静かで読みやすいドキュメント体験。
> 地（chrome）もアクセントも **無彩色** で統一し、装飾ではなく **タイポグラフィの階層と余白** で構造を伝える。
>
> 本書は「設計原則」と「デザイントークン定義」の正本です。実装（`globals.css` / 各コンポーネント）は本書に従います。
> 色・余白・角丸をコンポーネントに直書きせず、必ずここで定義したトークン（CSS 変数）を参照してください。

---

## 1. Design Principles（設計原則）

### 1.1 モノトーン厳守
- 地もアクセントも **無彩色（グレー〜黒）**。ブランド色・カラフルなアクセントは使わない。
- 色は「意味の伝達に色が **必須** なとき」だけ最小限に。原則としてゼロを目指す。
- ロール（customer / cast / admin）は **色で区別しない**。→ 1.5 参照。

### 1.2 余白でリズムを作る
- 構造は **罫線や影ではなく空間** で示す。境界線は極薄、影は原則なし。
- 詰め込まない。行間・段落間・見出し前後にゆとりを持たせ、「紙の余白」を意識する。

### 1.3 紙のような静けさ
- 文字色は **温かい中立グレー（`#37352F`）**。純黒でも青みグレーでもない。
- 高コントラストで読みやすいが、硬すぎない。長文マニュアルを疲れずに読める質感を優先。

### 1.4 装飾より内容
- 見出しにアクセントバー・下線などの飾りを **足さない**。
- 序列は **フォントサイズ・ウェイト・余白** だけで表現する。
- 絵文字を UI・見出しに使わない（意味のあるアイコンは SVG で）。

### 1.5 色に依存しないロール識別
- ロールは **ラベル・パンくず・URL・ページ見出し** で示す（旧 `data-role` 由来のブランド色は撤廃）。
- 色覚に依存しないため、アクセシブルかつモノトーン方針と両立する。

### 1.6 アクセシビリティ
- 本文テキストは **WCAG AA 以上** のコントラストを確保。
- フォーカスは常時可視（`:focus-visible` アウトライン）。
- ライト / ダーク / 印刷を **すべて** 破綻なく対応する。

---

## 2. Color Tokens（カラートークン）

CSS 変数名は既存実装（`--background` / `--surface` / `--foreground` / `--muted` / `--line` / `--accent*` …）を
維持します。`--accent` は **ロール別の上書きをやめ、単一の無彩色** に固定します。

> **重要**: `[data-role="customer|cast|admin"]` によるアクセント色上書き（teal / pink / indigo 計 6 行）は
> **すべて撤廃** します。`--accent` はテーマ内で 1 値のみ。

### 2.1 Light（既定）

| トークン | 値 | 用途 |
|---|---|---|
| `--background` | `#ffffff` | ページ背景 |
| `--surface` | `#ffffff` | カード・パネルの面 |
| `--surface-2` | `#f7f7f5` | サイドバー等の淡い地（Notion `rgb(251,251,250)` 相当） |
| `--surface-3` | `#efefec` | ホバー面・一段沈んだ面 |
| `--foreground` | `#37352F` | 本文テキスト（温かいダークグレー） |
| `--muted` | `#6b675e` | 補助テキスト（`rgba(55,53,47,.65)` 相当） |
| `--muted-2` | `#9b978d` | 最も控えめなテキスト（キャプション・マーカー） |
| `--line` | `rgba(55,53,47,0.09)` | 極薄罫線（既定の境界） |
| `--line-strong` | `rgba(55,53,47,0.16)` | 強めの境界（画像枠・入力枠） |
| `--accent` | `#37352F` | = 前景。強調・操作系の基準色 |
| `--accent-ink` | `#1f1e1b` | リンク・操作テキスト（ほぼ黒） |
| `--accent-hover` | `#000000` | ホバー時の濃色 |
| `--accent-soft` | `rgba(55,53,47,0.06)` | ホバー / 選択の淡い地 |
| `--accent-fg` | `#ffffff` | アクセント面上の文字（プライマリボタン等） |
| `--code-bg` | `#f4f4f2` | インラインコード地 |
| `--code-fg` | `#37352F` | インラインコード文字（無彩色。旧マゼンタ廃止） |
| `--pre-bg` | `#f7f7f5` | コードブロック地（濃紺やめ・淡グレー） |
| `--pre-fg` | `#37352F` | コードブロック文字 |
| `--shadow-sm` | `none`（境界線で代替） | 通常要素に影は使わない |
| `--shadow` | `0 8px 28px rgba(15,23,42,.08)` | オーバーレイ（モーダル/ドロワー/ポップオーバー）のみ |

### 2.2 Dark（Notion dark 準拠）

| トークン | 値 |
|---|---|
| `--background` | `#191919` |
| `--surface` | `#202020` |
| `--surface-2` | `#262626` |
| `--surface-3` | `#2f2f2f` |
| `--foreground` | `#e6e6e4`（`rgba(255,255,255,.81)` 相当） |
| `--muted` | `#9b9a97` |
| `--muted-2` | `#6f6e6b` |
| `--line` | `rgba(255,255,255,0.094)` |
| `--line-strong` | `rgba(255,255,255,0.16)` |
| `--accent` | `#e6e6e4` |
| `--accent-ink` | `#ffffff` |
| `--accent-hover` | `#ffffff` |
| `--accent-soft` | `rgba(255,255,255,0.07)` |
| `--code-bg` | `#2a2a2a` |
| `--pre-bg` | `#202020` |
| `--pre-fg` | `#e6e6e4` |
| `--shadow` | `0 10px 30px rgba(0,0,0,.5)`（オーバーレイのみ） |

### 2.3 選択・フォーカス
- `::selection` は **無彩色**（`rgba(55,53,47,0.12)` / ダーク `rgba(255,255,255,0.14)`）。
- `:focus-visible` は `2px solid var(--accent)` + `outline-offset: 2px`（無彩色の明快なリング）。

---

## 3. Typography（タイポグラフィ）

### 3.1 フォント
- 欧文/数字: **Inter**（`--font-inter`）、等幅: **Roboto Mono**（`--font-roboto-mono`）を維持。
- 和文は Web フォントを読み込まず、OS フォールバックへ:
  `"Hiragino Kaku Gothic ProN", "Hiragino Sans", "Yu Gothic", "YuGothic", "Noto Sans JP", "Meiryo"`。
- *（任意）* より Notion 的にするなら、先頭に `-apple-system, BlinkMacSystemFont, "Segoe UI"` を置く
  **システム UI フォント優先** も選択肢。導入する場合は本節を更新する。

### 3.2 スケール
- 本文: `16px` / `line-height: 1.7`。
- 見出し:

| 要素 | サイズ | ウェイト | letter-spacing |
|---|---|---|---|
| H1 | `2rem` | 700 | `-0.01em` |
| H2 | `1.5rem` | 600 | `0` 〜 `-0.005em` |
| H3 | `1.25rem` | 600 | `0` |

- **タイトな字詰めを緩和**する（旧 `-0.02em` は詰まりすぎ）。日本語では特に詰めない。

### 3.3 見出しの装飾
- **H2 の左アクセントバー（`::before`）と下線 `border-bottom` を撤廃**。
- 序列は **上下余白とウェイト** だけで表現する（例: H2 は `margin-top` を広く取る）。
- どうしても区切りが必要な箇所のみ、極薄 `--line` の下線を控えめに使う（原則は使わない）。

### 3.4 リンク
- 色はモノトーン: `--accent-ink` の文字 + **極薄下線**。
- hover では **下線を濃く**（色は変えない）。`text-underline-offset: 2px`。

---

## 4. Spacing & Layout（余白とレイアウト）

- **本文カラム幅**: `44rem`（≈704px）前後。Notion 本文（≈708px）に寄せ、余白感を強調する
  （現状 `max-w-[46rem]` から少し狭める）。
- **縦リズム**: 段落 `margin-bottom` と見出し前後の余白を広めに保つ。
- **角丸**: 全体にミニマル寄りへ縮小。

| トークン | 値 |
|---|---|
| `--radius-sm` | `4px` |
| `--radius-md` | `6px` |
| `--radius-lg` | `8px` |
| `--radius-xl` | `10px` |

- **Elevation**: 影は **原則不使用**。要素の区切りは `--line` の罫線で行う。
  影を使ってよいのは **オーバーレイ（モーダル / ドロワー / ポップオーバー）のみ**。

---

## 5. Components 規範

既存 12 コンポーネント（`AppShell` / `TopBar` / `SidebarNav` / `PageToc` / `Breadcrumbs` / `PrevNext` /
`SearchDialog` / `ThemeToggle` / `Callout` / `Figure` / `MdxContent`）への適用指針。

### 5.1 SidebarNav / PageToc
- アクティブ表現を **左ボーダー強調（`border-l-2 border-accent`）から背景ハイライト方式へ**。
  - アクティブ = `--surface-2` の面 + 前景を濃色に。ホバー = `--accent-soft`。
- ロール切替行の **丸ドット（`bg-accent`）は色を持たせない**（撤去、または無彩色の細い記号に）。

### 5.2 TopBar
- 下境界は罫線 1 本（`--line`）。`backdrop-blur` は維持。**影なし**。

### 5.3 Callout
- 4 色（note / tip / warning / step）→ **モノトーングレー地に統一**（`--surface-2` 系）。
- 種別の区別は **ラベル + SVG アイコン** で行う。
- **絵文字（ℹ️ 💡 ⚠️ 📝）は SVG アイコンに置換**する。
- *許容ルール*: 警告（warning）だけは、色を使わず **左罫を濃いグレー（`--line-strong`）** にする等で
  強弱を付けてよい。色相は導入しない。

### 5.4 Figure
- プレースホルダ **絵文字 🖼️ は SVG に置換**。
- 画像フレームは `--line-strong` の枠。影は使わない（使うなら極薄）。

### 5.5 トップのロールカード（`src/app/page.tsx`）
- 「上端アクセントバー + 頭文字バッジ + ピル + ホバーで動く矢印」の SaaS ランディング定番構図を、
  **無彩色の落ち着いたカード** に:
  - 余白を広く、境界は `--line` の罫線のみ、ホバーは面ハイライト（`--surface-2`）。
  - ブランド色バー・カラフルバッジは使わない。

### 5.6 コードブロック（MdxContent）
- 濃紺 `pre` → **淡グレー `pre`**（`--pre-bg` / `--pre-fg`）。インラインコードも無彩色。

### 5.7 共通
- すべての色・角丸・余白はトークン参照。ハードコード禁止。
- インタラクティブ要素は `:focus-visible` を必ず持つ。

---

## 6. Do / Don't（早見表）

**✅ Do**
- 無彩色（温かい中立グレー）で統一する
- 余白と極薄罫線で構造を示す
- タイポの階層（サイズ・ウェイト・余白）で序列を出す
- 意味のあるアイコンは SVG で描く
- 本文 AA 以上のコントラストを守る

**🚫 Don't**
- ブランド色 / カラフルなアクセントを使う
- グラデーションを使う
- 影を多用する（通常要素に影を付ける）
- 見出しに装飾バー・下線を足す
- 絵文字を UI・見出しに使う
- 字間をタイトに詰めすぎる（特に日本語）

---

## 付録: 移行メモ（次段階・本書の範囲外）

本書確定後、別タスクで以下を反映する:

- `globals.css`: トークン値の置換、`[data-role]` ロール別ブロックの削除、H2 の `::before` / `border-bottom` 撤去、
  コードブロック配色の無彩色化、角丸トークンの縮小。
- 各コンポーネント: アクティブ表現を背景ハイライトへ、丸ドットの無彩色化、影の除去。
- `Callout` / `Figure`: 絵文字 → SVG アイコン置換、Callout のモノトーン化。
- `src/app/page.tsx`: ロールカードの無彩色化。
- `CLAUDE.md` の「スタイル」節・「ロール別アクセント色」節を本書に整合させて更新。
